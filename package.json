{
  "name": "linkmink",
  "version": "1.0.0",
  "dependencies": {
    "@redocly/openapi-cli": "^1.0.0-beta.54",
    "latest": "^0.2.0"
  },
  "private": true,
  "scripts": {
    "start": "openapi preview-docs",
    "build": "openapi bundle -o dist",
    "dist": "npx redoc-cli build openapi/openapi.yaml && mv redoc-static.html index.html",
    "test": "openapi lint"
  }
}
